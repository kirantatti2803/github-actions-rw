name: Jfrog Artifactory

on:
    workflow_call:
        inputs:
            app:
                type: string
                description:  "Select application :  React-JS, Python or Java-Maven"
                required: true

jobs:
    jfrog-artifactory:
        name: Artifactory to Jfrog
        runs-on: ubuntu-latest
        steps:
            - name: Artifactory to Jfrog
              run: |
                echo " Artifacatory are stored in Jfrog Successfully"
